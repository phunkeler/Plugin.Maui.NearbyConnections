<BasePage
    x:Class="NearbyChat.Pages.AdvertisingPage"
    x:Name="this"
    x:TypeArguments="AdvertisingPageViewModel"
    Background="{AppThemeBinding Light={StaticResource LightBackgroundGradient}, Dark={StaticResource DarkBackgroundGradient}}"
    x:DataType="AdvertisingPageViewModel">

    <BasePage.Content>
        <Grid RowDefinitions="Auto,Auto,*"
              RowSpacing="{StaticResource SpacingXL}"
              Margin="{StaticResource SpacingXL}">
            <Button Style="{StaticResource BackButtonStyle}"
                    AutomationId="BackButton"
                    Command="{Binding BackCommand}"
                    HorizontalOptions="Start"
                    VerticalOptions="Start"/>
            <!-- Test-only hook for UI automation. Opacity="0" (fully zero
                 alpha) is pruned from Android's accessibility tree by the
                 OS itself — independent of MAUI's own accessibility mapping
                 — so UIAutomator2 can never find it via any locator, no
                 matter what AutomationId is set to. A small nonzero
                 opacity keeps it out of the accessibility-pruning path
                 while remaining visually imperceptible. -->
            <Label AutomationId="AdvertisingStatus"
                   Text="{Binding IsAdvertising}"
                   Opacity="0.01"/>
            <ConnectedDevicesChip
                Count="{Binding Connections.Count}"
                Command="{Binding NavigateToConnectionsCommand}"
                HorizontalOptions="End"
                VerticalOptions="Center"/>
            <FeatureControlCard
                VerticalOptions="Start"
                Grid.Row="1"
                ButtonAutomationId="ToggleAdvertising"
                FeatureIconBadgeStyle="{StaticResource FeatureIconBadgeAdvertiseStyle}"
                Title="Advertising Mode"
                ActiveSubtitle="Broadcasting"
                InactiveSubtitle="Not advertising"
                ActiveButtonText="Stop Advertising"
                InactiveButtonText="Start Advertising"
                ActiveButtonColor="{StaticResource AccentAdvertising}"
                InactiveButtonColor="{AppThemeBinding Light={StaticResource LightSurfaceSecondary}, Dark={StaticResource DarkSurfaceSecondary}}"
                FeatureIsActive="{Binding IsAdvertising}"
                Command="{Binding ToggleAdvertisingCommand}"/>

            <!-- Page-level failure sits under the toggle it belongs to, not in an alert: a modal
                 steals the screen from the button the user is about to tap again. -->
            <VerticalStackLayout
                Grid.Row="1"
                VerticalOptions="End"
                Spacing="2"
                IsVisible="{Binding HasFailed}">
                <Label
                    AutomationId="AdvertisingFailure"
                    LineBreakMode="WordWrap"
                    FontSize="{StaticResource FontSizeSM}"
                    FontAttributes="Bold"
                    TextColor="{StaticResource StatusError}"
                    Text="{Binding FailureReason}"/>
                <Label
                    LineBreakMode="WordWrap"
                    FontSize="{StaticResource FontSizeSM}"
                    TextColor="{AppThemeBinding Light={StaticResource LightTextSecondary}, Dark={StaticResource DarkTextSecondary}}"
                    IsVisible="{Binding HasRemedy}"
                    Text="{Binding FailureRemedy}"/>
            </VerticalStackLayout>

            <CollectionView
                Margin="0,-8,0,0"
                Grid.Row="2"
                ItemsSource="{Binding AdvertisedDevices}"
                VerticalScrollBarVisibility="Never"
                HorizontalScrollBarVisibility="Never">
                <CollectionView.Header>
                    <Label
                        Margin="{StaticResource SpacingSM}"
                        IsVisible="{Binding AdvertisedDevices.Count, Converter={StaticResource intToBoolConverter}}"
                        Text="Nearby Devices"
                        Style="{StaticResource ListHeader}"/>
                </CollectionView.Header>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="AdvertisedDeviceViewModel">
                        <Border Style="{StaticResource FeatureNavigationCardBorderStyle}">
                            <Grid RowDefinitions="*, *, Auto"
                                  ColumnDefinitions="*, *"
                                  RowSpacing="{StaticResource SpacingSM}">
                                <Label
                                    HorizontalOptions="Start"
                                    Grid.ColumnSpan="2"
                                    Style="{StaticResource CardTitle}"
                                    Text="{Binding DisplayName}"/>

                                <Border
                                    Grid.ColumnSpan="2"
                                    Stroke="Transparent"
                                    WidthRequest="20"
                                    HeightRequest="20"
                                    HorizontalOptions="End"
                                    StrokeShape="RoundRectangle 10"
                                    Background="{StaticResource LightTextQuaternary}">
                                    <Image
                                        WidthRequest="10"
                                        HeightRequest="10">
                                        <Image.Source>
                                            <FontImageSource
                                                FontFamily="NearbyChatIcons"
                                                Glyph="{StaticResource icon-down}"
                                                Color="White"/>
                                        </Image.Source>
                                    </Image>
                                </Border>
                                <!-- The failure reason takes the subtitle's place rather than
                                     adding a row, so a failed row is the same height as any
                                     other and the list does not jump. -->
                                <Label
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    HorizontalOptions="Start"
                                    Style="{StaticResource CardSubtitle}"
                                    IsVisible="{Binding HasFailed, Converter={toolkit:InvertedBoolConverter}}"
                                    Text="{Binding ReceivedAt, Converter={StaticResource relativeTimeConverter}, Mode=OneWay}"/>
                                <Label
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    HorizontalOptions="Start"
                                    LineBreakMode="WordWrap"
                                    FontSize="{StaticResource FontSizeSM}"
                                    TextColor="{StaticResource StatusError}"
                                    IsVisible="{Binding HasFailed}"
                                    AutomationId="{Binding Id, StringFormat='AcceptFailure_{0}'}"
                                    Text="{Binding FailureReason}"/>
                                <Button
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    HeightRequest="44"
                                    Text="Decline"
                                    AutomationId="{Binding Id, StringFormat='Decline_{0}'}"
                                    Command="{Binding DeclineCommand}"
                                    BackgroundColor="{AppThemeBinding Light={StaticResource LightSurfaceSecondary}, Dark={StaticResource DarkSurfaceSecondary}}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"
                                    FontAttributes="Bold"
                                    CornerRadius="12"
                                    FontSize="{StaticResource FontSizeSM}"/>
                                <Button
                                    Grid.Row="2"
                                    Grid.Column="1"
                                    HeightRequest="44"
                                    Text="Accept"
                                    AutomationId="{Binding Id, StringFormat='Accept_{0}'}"
                                    Command="{Binding AcceptCommand}"
                                    BackgroundColor="{StaticResource AccentDiscovery}"
                                    TextColor="{AppThemeBinding Light={StaticResource LightTextPrimary}, Dark={StaticResource DarkTextPrimary}}"
                                    FontAttributes="Bold"
                                    CornerRadius="12"
                                    FontSize="{StaticResource FontSizeSM}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                <CollectionView.EmptyView>
                    <VerticalStackLayout Spacing="{StaticResource SpacingLG}">
                        <Image
                            x:Name="AntennaIcon"
                            WidthRequest="56"
                            HeightRequest="56">
                            <Image.Source>
                                <FontImageSource x:Name="AntennaIconSource"
                                                 FontFamily="NearbyChatIcons"
                                                 Glyph="{StaticResource icon-antenna}"
                                                 Color="{AppThemeBinding Light={StaticResource LightTextQuaternary}, Dark={StaticResource DarkTextQuaternary}}"/>
                            </Image.Source>
                        </Image>
                        <Label
                            TextColor="{AppThemeBinding Light={StaticResource LightTextQuaternary}, Dark={StaticResource DarkTextQuaternary}}"
                            HorizontalTextAlignment="Center">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding IsAdvertising}"
                                             Value="False">
                                    <Setter Property="Text"
                                            Value="Start advertising to be discoverable"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding IsAdvertising}"
                                             Value="True">
                                    <Setter Property="Text"
                                            Value="Waiting for connection requests..."/>
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </Grid>
    </BasePage.Content>
</BasePage>