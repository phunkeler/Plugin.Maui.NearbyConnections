<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<TargetFrameworks>$(MobileTargetFrameworks)</TargetFrameworks>
    <TargetFrameworks Condition="$(IsTestProject) == true">$(TargetFrameworks);$(NetVersion)</TargetFrameworks>
    <UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Maui.Controls" /> <!-- TODO: Try to remove this in favor of something more targeted -->
    <PackageReference Include="Microsoft.SourceLink.GitHub" PrivateAssets="All" />
    <PackageReference Include="System.Reactive" />
    <PackageReference Include="Xamarin.GooglePlayServices.Nearby" Condition="'$(IsTargetPlatformAndroid)' == 'true'" />
	</ItemGroup>

	<ItemGroup>
	  <Folder Include="Events/Adapters/NearbyDeviceLost/" />
	</ItemGroup>

</Project>
